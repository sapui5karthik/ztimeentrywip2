<mvc:View
    controllerName="com.chappota.wippoc2.wipproject2.wip2.controller.App"
    displayBlock="true"
    xmlns="sap.m"
    xmlns:mvc="sap.ui.core.mvc">

    <Shell id="shell" appWidthLimited="false">
        <App
            id="app"
            busy="{appView>/busy}"
            busyIndicatorDelay="{appView>/delay}"/>
    </Shell>

</mvc:View>